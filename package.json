{
  "name": "gauravi-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "audit:privacy": "node scripts/audit-tracked-files.mjs",
    "preview": "vercel dev --local",
    "test": "node --test tests/*.test.mjs",
    "test:e2e": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.1",
    "cheerio": "^1.2.0",
    "vercel": "^59.1.4"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.118.0",
    "@vercel/blob": "^2.8.0"
  }
}
