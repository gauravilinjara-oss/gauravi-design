/* ───────────────────────────────────────────────────────────
   buttons.js — air.inc-style flat .ink-btn is pure CSS now.
   The old SVG turbulence ink-fill + magnetic pull have been retired
   to match air's calm, flat buttons. Kept as a no-op so existing
   <script src="buttons.js" defer> tags don't 404.
   ─────────────────────────────────────────────────────────── */
/* intentionally empty */
